# Static marketing site (hireshelby.com).
#
# Build from the repository root:
#   docker build -f Dockerfile.site -t hireshelby-site .

FROM nginx:1.29-alpine

COPY site/ /usr/share/nginx/html/
COPY deploy/railway/site-nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 8080
